import { View, Text, StyleSheet } from 'react-native';

export default function exportImport() {
	return (
		<View>
			<Text>Export and Import Page</Text>
		</View>
	);
}

const styles = StyleSheet.create({});
